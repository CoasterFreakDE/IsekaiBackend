package de.coasterfreak.isekaibackend


/**
 * The main method of the application.
 *
 * This method is the entry point for the program execution. It creates an instance of the `IsekaiBackend` class and starts the backend service.
 *
 * It does not accept any parameters and does not return any value.
 */
fun main() {
    IsekaiBackend()
}